# 🚀 Быстрый старт: Проверка безопасности Bitrix

## Что это?

Автоматизированная система сканирования безопасности для Bitrix CMS с использованием профессиональных инструментов:
- ✅ OWASP ZAP - веб-уязвимости
- ✅ Nikto - веб-сервер
- ✅ Trivy - Docker образы
- ✅ Bitrix Security Check - специфичные проверки

## 1️⃣ Быстрое сканирование (5-10 минут)

```bash
./security-scan.sh quick
```

**Проверяет:**
- XSS, SQL injection, CSRF
- Конфигурация nginx
- Права доступа к файлам Bitrix
- PHP в upload директории
- Резервные копии

## 2️⃣ Полное сканирование (30-60 минут)

```bash
./security-scan.sh full
```

**Дополнительно проверяет:**
- Полное сканирование OWASP ZAP
- SSL/TLS конфигурация
- CVE в Docker образах
- Composer зависимости

## 3️⃣ Просмотр результатов

```bash
# Сводный отчет
open security-reports/summary-*.html

# Bitrix проверки
cat security-reports/bitrix-security.txt

# OWASP ZAP
open security-reports/zap-baseline-report.html
```

## 📊 Интерпретация результатов

| Уровень | Что делать |
|---------|------------|
| 🔴 **Critical** | Исправить НЕМЕДЛЕННО |
| 🟠 **High** | Исправить в течение недели |
| 🟡 **Medium** | Запланировать исправление |
| 🟢 **Low** | Рассмотреть при возможности |

## 🔧 Отдельные сканеры

```bash
# Только OWASP ZAP
docker compose -f docker-compose.security.yml run --rm owasp-zap-baseline

# Только Nikto
docker compose -f docker-compose.security.yml run --rm nikto

# Только Bitrix check
docker compose -f docker-compose.security.yml run --rm bitrix-security-check
```

## ⚡ Частые проблемы

### "Контейнеры не запущены"

```bash
docker compose -f docker-compose.bitrix.yml --profile local up -d
```

### "Отчеты не генерируются"

Проверьте права:
```bash
chmod +x security-scan.sh
chmod +x security-scripts/bitrix-security-check.sh
```

## 📚 Полная документация

Смотрите [SECURITY.md](SECURITY.md) для подробной информации.

## ⚠️ Важно

- Отчеты содержат чувствительную информацию
- Не публикуйте их в открытом доступе
- Добавлено в `.gitignore`
