# 🚀 Bitrix Docker Environment

Production-ready Docker окружение для 1С-Битрикс: Управление сайтом

## ✨ Ключевые особенности

### 🎯 Единый контейнер Bitrix
- **Supervisor** управляет всеми процессами
- **PHP-FPM** для обработки веб-запросов
- **Cron** для агентов Битрикс
- **PHP-CLI** для консольных команд
- Все в одном контейнере для упрощенного управления

### 🏗️ Архитектура
- **PHP 8.3** (поддержка 7.4 при необходимости)
- **MySQL 8.0** оптимизирован под Битрикс
- **Redis** для кэширования данных и сессий
- **Memcached** для дополнительного кэша
- **Nginx** с оптимизацией под Битрикс

### 📊 Мониторинг
- **Grafana** - визуализация метрик и логов
- **Prometheus** - сбор метрик
- **Loki** - централизованное логирование (1 год retention)
- **Promtail** - сбор логов из всех контейнеров
- **Logrotate** - ротация файлов логов (7 дней)

### 🛡️ Безопасность
- **Fail2ban** - защита от брутфорса
- **ModSecurity WAF** - веб-файрвол
- **Rate limiting** - защита от DDoS
- **Security headers** - XSS, CSRF защита
- **Non-root execution** - контейнеры работают без root

### 🔄 Окружения
Готовые конфигурации для:
- **local** - локальная разработка
- **dev** - разработческий сервер
- **test** - тестирование
- **prod** - production с максимальной оптимизацией

## 🏗️ Архитектура системы

```
Internet → Nginx (80/443)
              ↓
     Bitrix Container (единый контейнер)
     ┌──────────────────────────┐
     │      Supervisor          │
     ├──────────────────────────┤
     │ PHP-FPM | Cron | CLI     │
     └──────────────────────────┘
              ↓
     MySQL | Redis | Memcached
              ↓
     Grafana ← Loki ← Promtail
```

## 💻 Требования

- **Docker**: 20.10+
- **Docker Compose**: 2.0+
- **RAM**: минимум 4GB, рекомендуется 8GB+
- **CPU**: минимум 2 cores
- **Disk**: 20GB свободного места

### Поддерживаемые платформы
- ✅ Linux (Ubuntu, Debian, CentOS, Fedora)
- ✅ macOS (Intel и Apple Silicon)
- ✅ Windows 10/11 (с WSL 2)

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd b-docker
```

### 2. Настройка окружения

#### Для локальной разработки:
```bash
cp .env.local.bitrix .env
nano .env  # Настройте DOMAIN и другие параметры
```

#### Для production:
```bash
cp .env.prod.bitrix .env
nano .env  # ОБЯЗАТЕЛЬНО смените все пароли!
```

### 3. Автоматическая настройка

```bash
# Автоматически определяет CPU, RAM и генерирует оптимальные конфиги
./auto-setup-bitrix.sh local

# Для production
./auto-setup-bitrix.sh prod
```

### 4. Создание директорий

```bash
mkdir -p www/upload www/local www/bitrix/cache
mkdir -p volume/logs volume/mysql/dump
mkdir -p ssl backups
```

### 5. Запуск

```bash
# Локальная разработка
docker compose -f docker-compose.bitrix.yml --profile local up -d

# Production со всеми профилями
docker compose -f docker-compose.bitrix.yml \
  --profile prod \
  --profile monitoring \
  --profile security \
  up -d
```

### 6. Установка Битрикс

Откройте в браузере: `http://your-domain.local`

Параметры подключения к БД:
- **Сервер**: `mysql`
- **База данных**: значение из `.env` (`DB_NAME`)
- **Пользователь**: значение из `.env` (`DB_USERNAME`)
- **Пароль**: значение из `.env` (`DB_PASSWORD`)

## 📁 Структура проекта

```
b-docker/
├── docker-compose.bitrix.yml    # Основной compose файл
├── auto-setup-bitrix.sh         # Автонастройка под железо
├── Makefile                     # Команды управления
│
├── .env.local.bitrix           # Конфиг для local
├── .env.dev.bitrix             # Конфиг для dev
├── .env.test.bitrix            # Конфиг для test
├── .env.prod.bitrix            # Конфиг для production
├── .env                         # Активный конфиг (создается вами)
│
├── docker/
│   ├── php/
│   │   ├── base/               # Базовые образы PHP
│   │   └── bitrix/             # Единый Bitrix контейнер
│   │       ├── Dockerfile
│   │       ├── supervisord.conf
│   │       ├── docker-entrypoint.sh
│   │       └── healthcheck.sh
│   ├── nginx/
│   ├── mysql/
│   ├── fail2ban/
│   └── modsecurity/
│
├── config/
│   ├── cron/crontab            # Битрикс cron задачи
│   ├── grafana/dashboards/     # Готовые дашборды
│   ├── loki/                   # Loki конфигурация
│   ├── mysql/                  # MySQL конфиги для окружений
│   ├── nginx/                  # Nginx конфигурация
│   ├── prometheus/rules/       # Alerting правила
│   ├── promtail/               # Promtail конфигурация
│   ├── redis/                  # Redis конфигурация
│   ├── logrotate/              # Ротация логов
│   └── supervisor/conf/        # Supervisor программы
│
├── www/                        # Код Битрикс
│   ├── upload/                 # Файлы загрузок (volume)
│   ├── local/                  # Локальные модули (volume)
│   ├── bitrix/
│   │   └── cache/             # Кэш Битрикс (volume)
│   └── index.php
│
├── volume/                     # Persistent data
│   ├── logs/                  # Логи всех сервисов
│   │   ├── nginx/
│   │   ├── php-fpm/
│   │   ├── mysql/
│   │   ├── cron/
│   │   ├── supervisor/
│   │   └── bitrix/
│   ├── mysql/
│   │   └── dump/              # Директория для дампов
│   └── grafana/
│
├── backups/                    # Бэкапы
└── ssl/                        # SSL сертификаты
```

## ⚙️ Основные компоненты

### Bitrix Container
Единый контейнер с Supervisor управляющим всеми процессами:
- **PHP-FPM** (порт 9000) - обработка веб-запросов
- **Cron** - выполнение агентов Битрикс каждые 5 минут
- **PHP-CLI** - консольные команды

### База данных
- **MySQL 8.0** (совместимость с ARM Mac)
- Оптимизированные конфигурации для каждого окружения
- Логи slow queries и errors идут в Grafana

### Кэширование
- **Redis** - кэш данных, сессии
- **Memcached** - дополнительное кэширование
- **OPcache** - кэширование PHP кода

### Мониторинг
- **Grafana** (порт 3000) - визуализация
- **Prometheus** (порт 9090) - метрики
- **Loki** (порт 3100) - логи с 1 год retention
- **Promtail** - сбор логов

### Безопасность (опционально)
- **Fail2ban** - защита от атак
- **ModSecurity** - WAF с OWASP rules
- **Rate limiting** в Nginx
- **Security headers**

## 🎮 Управление

### Docker Compose команды

```bash
# Запуск
docker compose -f docker-compose.bitrix.yml up -d

# Остановка
docker compose -f docker-compose.bitrix.yml down

# Перезапуск
docker compose -f docker-compose.bitrix.yml restart bitrix

# Логи
docker compose -f docker-compose.bitrix.yml logs -f bitrix

# Вход в контейнер
docker compose -f docker-compose.bitrix.yml exec bitrix bash

# Статус процессов Supervisor
docker compose -f docker-compose.bitrix.yml exec bitrix supervisorctl status
```

### Makefile команды

```bash
# Инициализация
make init-local              # Для локальной разработки
make init-prod               # Для production

# Управление
make up                      # Запуск
make down                    # Остановка
make restart                 # Перезапуск
make logs                    # Логи

# Бэкапы
make backup-db               # Бэкап базы данных
make backup-files            # Бэкап файлов
make backup-full             # Полный бэкап

# Очистка
make clean-cache             # Очистка кэша Битрикс
make clean-logs              # Очистка логов
```

## 📊 Мониторинг и логирование

### Grafana

**URL**: http://localhost:3000
**Логин**: admin / пароль из `.env`

**Дашборды:**
- Security Dashboard - мониторинг безопасности и атак
- Nginx Security Analysis - анализ трафика
- System Metrics - CPU, RAM, Disk
- MySQL Metrics - производительность БД
- Redis Metrics - кэш статистика

### Логи

Все логи собираются в двух местах:

1. **Файлы** (retention 7 дней):
   ```
   ./volume/logs/
   ├── nginx/          - access.log, error.log
   ├── php-fpm/        - error.log, slow.log
   ├── mysql/          - error.log, slow.log
   ├── cron/           - cron.log (агенты Битрикс)
   ├── supervisor/     - supervisord.log
   └── bitrix/         - битрикс логи
   ```

2. **Loki** (retention 1 год):
   - Все логи из контейнеров
   - Поиск через Grafana Explore
   - Алерты в Prometheus

### Поиск в логах (Grafana → Explore → Loki)

```logql
# Ошибки PHP
{container_name="bitrix"} |= "error"

# Медленные запросы MySQL
{job="mysql"} |= "slow"

# Агенты Битрикс (cron)
{job="cron"}

# Блокировки Fail2ban
{job="fail2ban"} |= "Ban"
```

## 🛡️ Безопасность

### Security Headers (автоматически в Nginx)
- `X-Frame-Options: SAMEORIGIN`
- `X-Content-Type-Options: nosniff`
- `X-XSS-Protection: 1; mode=block`
- `Referrer-Policy: no-referrer-when-downgrade`

### Rate Limiting
- Login/Admin: 5 запросов/минуту
- API: 10 запросов/секунду
- Static: 30 запросов/секунду
- General: 2 запроса/секунду

### SSL/TLS

**Автоматические сертификаты (Let's Encrypt):**
```bash
# В .env установите:
SSL=free
EMAIL=your@email.com
```

**Собственные сертификаты:**
```bash
# В .env установите:
SSL=self

# Поместите сертификаты в:
config/nginx/ssl/${DOMAIN}/fullchain.pem
config/nginx/ssl/${DOMAIN}/privkey.pem
```

### Fail2ban (опционально)

```bash
# Запуск
docker compose --profile security up -d

# Статус
docker compose exec fail2ban fail2ban-client status

# Заблокированные IP
docker compose exec fail2ban fail2ban-client status nginx-brute

# Разблокировать IP
docker compose exec fail2ban fail2ban-client unban 192.168.1.100
```

## ⚡ Производительность

### Auto-setup скрипт

Автоматически определяет железо и генерирует оптимальные конфигурации:

```bash
./auto-setup-bitrix.sh local
./auto-setup-bitrix.sh prod
```

**Что настраивается:**
- MySQL buffer pool (60% RAM)
- MySQL connections (50 на CPU core)
- PHP-FPM workers (5 на CPU core)
- Redis maxmemory (25% RAM)
- Nginx worker processes (по количеству CPU)

### Оптимизация Битрикс

1. **Композитный кэш**:
   - Админка → Настройки → Производительность
   - Включить "Композитный сайт"

2. **Использование Redis**:
   - Уже настроен для кэша и сессий
   - Проверить в .settings.php

3. **OPcache**:
   - Включен в prod
   - Отключен в local для актуального кода

## 💾 Бэкапы

### Автоматические бэкапы

Настраиваются в `.env`:
```bash
BACKUP_SCHEDULE_DB=0 2 * * *      # БД в 2:00 каждый день
BACKUP_SCHEDULE_FILES=0 3 * * *   # Файлы в 3:00 каждый день
BACKUP_RETENTION_DAYS=30          # Хранить 30 дней
```

### Ручные бэкапы

```bash
# База данных
docker compose exec mysql mysqldump -ubitrix -p${DB_PASSWORD} ${DB_NAME} \
  | gzip > ./backups/db_$(date +%Y%m%d_%H%M%S).sql.gz

# Файлы
tar -czf ./backups/files_$(date +%Y%m%d_%H%M%S).tar.gz \
  ./www/upload ./www/local

# Или через Makefile
make backup-db
make backup-files
make backup-full
```

### Восстановление

```bash
# База данных
gunzip < ./backups/db_20250101_020000.sql.gz | \
  docker compose exec -T mysql mysql -ubitrix -p${DB_PASSWORD} ${DB_NAME}

# Файлы
tar -xzf ./backups/files_20250101_030000.tar.gz -C ./www/
```

## 🔧 Troubleshooting

### 502 Bad Gateway

```bash
# Проверьте PHP-FPM
docker compose exec bitrix supervisorctl status php-fpm

# Перезапустите
docker compose exec bitrix supervisorctl restart php-fpm

# Логи
docker compose logs bitrix
```

### Нет подключения к БД

```bash
# Проверьте MySQL
docker compose exec mysql mysqladmin ping

# Проверьте пароль в .env
grep DB_PASSWORD .env

# Проверьте .settings.php
cat www/bitrix/.settings.php | grep -A 5 connections
```

### Права доступа на файлы

```bash
# Узнайте свой UID/GID
id -u  # UID
id -g  # GID

# Установите в .env
UID=1000
GID=1000

# Пересоздайте контейнеры
docker compose down && docker compose up -d --build

# Исправьте права
sudo chown -R 1000:1000 ./www/upload
sudo chmod -R 777 ./www/upload ./www/bitrix/cache
```

### Медленная работа

**Чеклист:**
1. ✅ OPcache включен (prod)
2. ✅ MySQL buffer pool настроен
3. ✅ Композитный кэш Битрикса включен
4. ✅ Redis работает

```bash
# Медленные запросы MySQL
docker compose exec mysql tail -f /var/log/mysql/slow.log

# Медленные запросы PHP
docker compose logs bitrix | grep slow
```

## ❓ FAQ

### Какая версия PHP?
**PHP 8.3** по умолчанию. Для старых модулей можно использовать PHP 7.4.

### Как переключить PHP версию?
```bash
# В .env измените:
PHP_VERSION=7.4

# Пересоберите
docker compose build --no-cache bitrix
docker compose up -d bitrix
```

### Работает на macOS Apple Silicon?
**Да!** Все образы поддерживают ARM64. MySQL настроен без O_DIRECT для совместимости.

### Можно ли использовать на Windows?
**Да!** Но только с WSL 2:
```powershell
wsl --install
# Затем работайте из WSL терминала
```

### Где хранятся логи?
- **Файлы**: `./volume/logs/` (7 дней)
- **Loki**: через Grafana (1 год)

### Как настроить SMTP?
Для production настройте в:
- Админка → Настройки → Почтовые события

Для local/dev используйте MailHog (уже настроен):
- Web UI: http://localhost:8025

## 📋 Production Checklist

Перед запуском на production:

- [ ] Смените ВСЕ пароли в `.env`
- [ ] Установите `DEBUG=0`
- [ ] Настройте SSL (`SSL=free` для Let's Encrypt)
- [ ] Настройте firewall (только 80, 443, 22)
- [ ] Настройте автоматические бэкапы
- [ ] Настройте внешнее хранилище бэкапов (S3, rsync)
- [ ] Настройте алерты в Grafana
- [ ] Проверьте права на директории
- [ ] Включите композитный кэш Битрикса
- [ ] Настройте мониторинг через Grafana

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте [Troubleshooting](#-troubleshooting)
2. Проверьте [FAQ](#-faq)
3. Изучите логи: `docker compose logs -f`
4. Проверьте статус: `docker compose exec bitrix supervisorctl status`

## 📄 Лицензия

MIT License

## 🌟 Итоговые возможности

**Что включено:**
- ✅ Полный веб-стек: Nginx + PHP + MySQL + Redis + Memcached
- ✅ Единый Bitrix контейнер с Supervisor
- ✅ Мониторинг: Grafana + Prometheus + Loki
- ✅ Логирование: 1 год в Loki + 7 дней в файлах
- ✅ Безопасность: Rate limiting + Security headers + Fail2ban + ModSecurity
- ✅ Автонастройка под железо
- ✅ Готовые конфиги для local/dev/test/prod
- ✅ Кросс-платформенность: Linux, macOS, Windows (WSL2)

**Production ready!** ✨
