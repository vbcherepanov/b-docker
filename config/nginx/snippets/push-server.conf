# =============================================================================
# BITRIX PUSH SERVER PROXY CONFIGURATION
# Websocket support for real-time notifications
# =============================================================================

# WebSocket upgrade headers
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

# Push server locations - include in main server block
# location /bitrix/sub/ {
#     include /etc/nginx/snippets/push-server-proxy.conf;
# }
