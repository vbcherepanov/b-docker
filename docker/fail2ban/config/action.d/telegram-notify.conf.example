# Пример custom action для отправки уведомлений в Telegram
# Переименуйте в .conf для использования

[Definition]

# Команда при бане IP
actionban = curl -s -X POST https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/sendMessage \
            -d chat_id=<TELEGRAM_CHAT_ID> \
            -d text="🚫 Fail2Ban: Забанен IP <ip> за атаку <name>. Время бана: <bantime>s"

# Команда при разбане IP
actionunban = curl -s -X POST https://api.telegram.org/bot<TELEGRAM_BOT_TOKEN>/sendMessage \
              -d chat_id=<TELEGRAM_CHAT_ID> \
              -d text="✅ Fail2Ban: IP <ip> разбанен для <name>"

# Параметры по умолчанию
[Init]
name = default
bantime = 600

# Использование:
# В jail.local добавьте:
# action = %(action_mw)s
#          telegram-notify[name=%(__name__)s]
