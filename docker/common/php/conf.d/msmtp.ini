# ============================================================================
# MSMTP Configuration for Production
# Используется только для production окружения
# Для local используется mailhog.ini с mhsendmail
# ============================================================================
[mail function]
sendmail_path = "/usr/bin/msmtp -C /etc/msmtp/msmtprc -t -i"
